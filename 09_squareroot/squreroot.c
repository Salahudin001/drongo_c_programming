#include <math.h>
#include <stdio.h>
#include <assert.h>

void main()
{
    int x = sqrt(49);
    int y = 5;
    int z = 45;
    int m = y + z;
   // printf("%I\n",x);
    //printf("%i\n", x);
    assert(m = 50);
}